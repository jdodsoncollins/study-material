title: Push and pop straps
steps:
  - caption: "Openers [ { ( go on the stack. Closers only look at the top."
    nodes: [ch, kind, push]
  - caption: ") matches (. Pop. { is still on top."
    nodes: [kind, pop, ch]
  - caption: "A mismatch, or a closer on an empty stack, is illegal. Leftover opens at the end are too."
    nodes: [pop, bad]
